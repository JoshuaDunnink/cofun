{
  "name": "cofun-website",
  "type": "module",
  "version": "1.0.0",
  "description": "CoFun   Spelend beter leren. Website voor scholen en onderwijsprofessionals.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "powershell -NoProfile -ExecutionPolicy Bypass -File ./deploy.ps1",
    "deploy:setup": "powershell -NoProfile -ExecutionPolicy Bypass -File ./deploy.ps1 -SetupCredentials",
    "deploy:cmd": "deploy.cmd",
    "astro": "astro",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "astro": "^5.5.0"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "prettier-plugin-astro": "^0.14.0"
  }
}
